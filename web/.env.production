# Build de producción (GitHub Pages) → apunta a la API self-hosted en la Raspberry.
# No es secreto: se hornea en el JS publicado. Lo lee `npm run build` (modo production).
PUBLIC_API_URL=https://poke.servegame.com
